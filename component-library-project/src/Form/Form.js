import React, { Component } from 'react';
import "./Form.css";

let Form = props => {
    let classList = "";
    let types = ['email', 'dropdown', 'counter', 'code']

    render() {
        return (
            <div>
                
            </div>
        );
    }
}

export default Form;
